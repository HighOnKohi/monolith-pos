/**
 * Shared TypeScript type definitions for Monolith.
 *
 * Only add types here that are genuinely shared across multiple modules.
 * Do not design the full POS domain model prematurely.
 */

// Reserved for future shared types.
// Example future additions: User, Role, ApiResponse<T>, etc.
export {}
